export class BootMain {

  public run():void {
    if (Game.rooms[0].controller) {
      switch(Game.rooms[0].controller.level) {
        case 1:
          break

        case 2:
          break

        case 3:
          break
      }
    }
  }
}
