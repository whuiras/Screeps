export abstract class AbstractRCLConstants {
  abstract readonly roads: number
  abstract readonly containers: number
  abstract readonly spawns: number
  abstract readonly extensions: number
  abstract readonly ramparts: number
  abstract readonly towers: number
  abstract readonly storage: number
  abstract readonly links: number
  abstract readonly coreLinks: number
  abstract readonly sourceLinks: number
  abstract readonly controllerLinks: number
  abstract readonly extractor: number
  abstract readonly labs: number
  abstract readonly terminal: number
  abstract readonly observer: number
  abstract readonly powerSpawn: number
}
